name: Build, publish and deploy to MonsterASP.NET
on: [push]

jobs:
  build_and_deploy:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup .NET 9
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 9.0
          
      - name: Install dependencies
        run: dotnet restore QiBalance.sln
        
      - name: Build
        run: dotnet publish ./QiBalance/QiBalance.csproj --configuration Release  --output ./publish  
      - name: Publish
        run: dotnet publish ./QiBalance/QiBalance.csproj --configuration Release --output ./publish 
        

      - name: Deploy to MonsterASP.NET via WebDeploy
        uses: rasmusbuchholdt/simply-web-deploy@2.1.0
        with:
          website-name: ${{ secrets.WEBSITE_NAME_QI }}
          server-computer-name: ${{ secrets.SERVER_COMPUTER_NAME_QI }}
          server-username: ${{ secrets.SERVER_USERNAME_QI }}
          server-password: ${{ secrets.SERVER_PASSWORD_QI }}
